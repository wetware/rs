[package]
name = "echo"
version = "0.1.0"
edition = "2021"

[workspace]  # standalone — not part of the host workspace

[lib]
crate-type = ["cdylib"]

[dependencies]
wasip2 = "1.0.2"
wit-bindgen = "0.41.0"
